# experiments/

Per-cycle copies of harness JSON after a **successful** `run-m1`.
The Hypothesis Graph (`agents/hypothesis_graph.json`) is the source of truth.
Do not treat this directory as memory; failed runs must not leave a success-looking folder.
